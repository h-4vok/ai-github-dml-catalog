# .env.example

# --- GitHub Configuration ---
# Personal Access Token with 'repo' scope is required.
GITHUB_TOKEN=your_github_pat

# --- CHOOSE ONE ---
# The name of the GitHub organization OR user account you want to scan.
# Comment out the one you are not using.
# GITHUB_ORG_NAME=Sextant-Solutions
GITHUB_USER_NAME=your-github-username
# ------------------

# (Optional) The specific branch to search in. If not set, the default branch is used.
GITHUB_BRANCH_NAME=main

# --- LLM Provider Configuration ---
# Choose your LLM provider. Supported values: 'ollama' or 'cloud'.
LLM_PROVIDER=ollama
# Timeout for LLM requests in milliseconds (e.g., 360000 for 6 minutes).
LLM_TIMEOUT_MS=360000

# --- Debug Configuration ---
# Set to 'true' to save snippets that were analyzed but contained no DML to a separate file.
SAVE_REJECTED_CANDIDATES=false

# --- Ollama Configuration (only used if LLM_PROVIDER is 'ollama') ---
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3

# --- Cloud LLM Configuration (only used if LLM_PROVIDER is 'cloud') ---
CLOUD_API_KEY=your_cloud_api_key
CLOUD_API_URL=https://api.openai.com/v1/chat/completions
CLOUD_MODEL=gpt-4o

# --- Scanner Configuration ---
SCANNER_FILE_EXTENSIONS=.php,.go,.java,.cs,.py,.rb,.js,.ts,.sql
