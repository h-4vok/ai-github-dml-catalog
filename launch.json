{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Bun App",
      "type": "bun",
      "request": "launch",
      "program": "${workspaceFolder}/src/main.ts",
      "cwd": "${workspaceFolder}",
      "watch": true
    }
  ]
}
